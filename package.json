{
  "name": "flask_app",
  "version": "1.0.0",
  "description": "First flask application.",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \" cd ./flask-server && python server.py\" \"cd ./client && npm start\" ",
    "install": "concurrently \" cd ./flask-server && pip install -r requirements.txt\" \"cd ./client && npm install\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
